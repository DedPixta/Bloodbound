
require('libraries/selection') -- Selection library (by Noya) provides player selection inspection and management from server lua
require('settings/game_settings') -- settings.lua is where you can specify many different properties for your game mode and is one of the core barebones files.
require('events') -- events.lua is where you can specify the actions to be taken when any event occurs and is one of the core barebones files.
require('core.filters')
require('core.gamemode')
require('core.listeners')
require('core.precache')
require('core/systems')
